# PML benchmark

This folder contains benchmark of our matrix ``determinant'' algorithm versus [PML library], on our usecase (very sparse polynomial matrix).


## Prerequisites

Requires NTL installed, e.g. with

```sh
$ sudo apt install libntl44 libntl-dev
```

PML library should either be set up, or will be cloned and compiled automatically.
